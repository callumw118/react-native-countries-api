# react-native-countries-api
Simple React Native app which uses the restcountries API. 
Can select countries by region with an integrated search function.
Clicking on the country will display its flag
